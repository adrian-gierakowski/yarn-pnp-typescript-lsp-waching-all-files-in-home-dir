// Type: inModuleT and press tab to trigger auto-complete
// TypeScript LSP will proceede to add all files in your home
// dir to it's watch list.
// On linux, you can confirm this by running `nix run` at the root of this repo,
// which will build and run [inotify-info](https://github.com/mikesart/inotify-info)
